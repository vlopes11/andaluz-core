extern crate andaluz_core;

use std::os::raw::c_char;
use andaluz_core::board::Board;

#[no_mangle]
pub fn solve() -> *mut c_char {
    /*
    let mut board = Board::new(8);
    let solved = board.solve();
    let jumps = board.jumps;

    if solved {
        format!("Signature '{}' reached after {} jumps!", board.signature, jumps)
    } else {
        format!("Solution failed after {} jumps!", jumps)
    }
    */
    let out = "Das, Ich!";
    CString::new(out.as_str())
    "Das, Ich!"
}
